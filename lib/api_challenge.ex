defmodule ApiChallenge do

  def api_calls do
    # 5 processes that each make an api call at the same time
  end

  def test_url do
    "https://api.weatherapi.com/v1/forecast.json?key=7514621f9a034b1b9a1170939231805&q=Salt_Lake_City&days=6&aqi=no&alerts=no"
  end
end
